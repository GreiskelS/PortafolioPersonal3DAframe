<form method="post" action="{{ path('app_web_proyect_delete', {'id': web_proyect.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ web_proyect.id) }}">
    <button class="btn">Delete</button>
</form>
