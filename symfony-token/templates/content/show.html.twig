{% extends 'base.html.twig' %}

{% block title %}Content{% endblock %}

{% block body %}
    <h1>Content</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ content.id }}</td>
            </tr>
            <tr>
                <th>Name_proyect</th>
                <td>{{ content.NameProyect }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ content.Description }}</td>
            </tr>
            <tr>
                <th>Proyect_type</th>
                <td>{{ content.ProyectType }}</td>
            </tr>
            <tr>
                <th>Git</th>
                <td>{{ content.Git }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_content_index') }}">back to list</a>

    <a href="{{ path('app_content_edit', {'id': content.id}) }}">edit</a>

    {{ include('content/_delete_form.html.twig') }}
{% endblock %}
